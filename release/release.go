// Package release exposes the distribution identity shared by Rin binaries,
// providers, and source SDK snapshots. Wire contracts keep their own versions.
package release

const (
	Version = "0.7.0"
	Status  = "preview"
)
